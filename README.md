# CED: Managed Services Pricign Calcualtor — Streamlit

This is a Streamlit app version of the pricing calculator (one-file app).

## Run locally

1. Create virtualenv (recommended)
   ```bash
   python -m venv .venv
   source .venv/bin/activate   # macOS / Linux
   .venv\Scripts\activate      # Windows (PowerShell)
